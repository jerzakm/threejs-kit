#ifdef USE_MAP

vec2 vMapUv = parallaxedUv;

#endif
#ifdef USE_ALPHAMAP

vec2 vAlphaMapUv = parallaxedUv;

#endif
#ifdef USE_LIGHTMAP

vec2 vLightMapUv = parallaxedUv;

#endif
#ifdef USE_AOMAP

vec2 vAoMapUv = parallaxedUv;

#endif
#ifdef USE_BUMPMAP

vec2 vBumpMapUv = parallaxedUv;

#endif
#ifdef USE_NORMALMAP

vec2 vNormalMapUv = parallaxedUv;

#endif
#ifdef USE_EMISSIVEMAP

vec2 vEmissiveMapUv = parallaxedUv;

#endif
#ifdef USE_METALNESSMAP

vec2 vMetalnessMapUv = parallaxedUv;

#endif
#ifdef USE_ROUGHNESSMAP

vec2 vRoughnessMapUv = parallaxedUv;

#endif
#ifdef USE_ANISOTROPYMAP

vec2 vAnisotropyMapUv = parallaxedUv;

#endif
#ifdef USE_CLEARCOATMAP

vec2 vClearcoatMapUv = parallaxedUv;

#endif
#ifdef USE_CLEARCOAT_NORMALMAP

vec2 vClearcoatNormalMapUv = parallaxedUv;

#endif
#ifdef USE_CLEARCOAT_ROUGHNESSMAP

vec2 vClearcoatRoughnessMapUv = parallaxedUv;

#endif
#ifdef USE_IRIDESCENCEMAP

vec2 vIridescenceMapUv = parallaxedUv;

#endif
#ifdef USE_IRIDESCENCE_THICKNESSMAP

vec2 vIridescenceThicknessMapUv = parallaxedUv;

#endif
#ifdef USE_SHEEN_COLORMAP

vec2 vSheenColorMapUv = parallaxedUv;

#endif
#ifdef USE_SHEEN_ROUGHNESSMAP

vec2 vSheenRoughnessMapUv = parallaxedUv;

#endif
#ifdef USE_SPECULARMAP

vec2 vSpecularMapUv = parallaxedUv;

#endif
#ifdef USE_SPECULAR_COLORMAP

vec2 vSpecularColorMapUv = parallaxedUv;

#endif
#ifdef USE_SPECULAR_INTENSITYMAP

vec2 vSpecularIntensityMapUv = parallaxedUv;

#endif
